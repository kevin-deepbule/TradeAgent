TradeAgent 当前系统架构图
========================

一、整体运行架构
----------------

┌─────────────────────────────────────────────────────────────────────────┐
│                              用户浏览器                                 │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │ HTTP / WebSocket
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Frontend：Vue 3 + Vite + ECharts                                        │
│                                                                         │
│ - K 线行情展示                                                          │
│ - 自选股与默认股票管理                                                  │
│ - 交易建议展示                                                          │
│ - 前端策略回测                                                          │
│ - 买卖标记与持仓区间绘制                                                │
└──────────────────────────────────┬──────────────────────────────────────┘
                                   │
                                   │ REST: /api/**
                                   │ WS:   /ws/stocks/**
                                   ▼
┌─────────────────────────────────────────────────────────────────────────┐
│ Backend：Spring Boot 多模块单体                                         │
│                                                                         │
│ 一个进程、一个可执行 Jar、一个统一 API 边界                             │
└──────────────────────┬───────────────────────────────┬──────────────────┘
                       │                               │
                       │ HTTP                          │ JDBC / MyBatis
                       ▼                               ▼
┌──────────────────────────────────┐    ┌─────────────────────────────────┐
│ AkShare Adapter                  │    │ PostgreSQL                      │
│ FastAPI + Python + pandas        │    │                                 │
│                                  │    │ - app_settings                  │
│ - 股票代码/名称解析              │    │ - watchlist                     │
│ - A 股历史 K 线                  │    │ - 后续研究数据                   │
│ - AkShare 数据适配               │    │                                 │
└──────────────────┬───────────────┘    └─────────────────────────────────┘
                   │ Python API
                   ▼
┌──────────────────────────────────┐
│ AkShare                          │
│                                  │
│ - A 股行情                       │
│ - 股票基础信息                   │
│ - 后续财报、公告等数据           │
└──────────────────────────────────┘

约束：浏览器只调用 Spring Boot 后端，不直接调用 AkShare Adapter。


二、后端多模块单体架构
----------------------

                         ┌──────────────────────────────┐
                         │ trade-app                    │
                         │                              │
                         │ - Spring Boot 启动入口       │
                         │ - 模块统一组装               │
                         │ - 健康检查                   │
                         │ - CORS                       │
                         │ - 全局异常处理               │
                         │ - application.properties    │
                         └──────┬──────────┬─────────┬──┘
                                │          │         │
                    ┌───────────┘          │         └───────────┐
                    ▼                      ▼                     ▼
┌────────────────────────────┐  ┌──────────────────────┐  ┌──────────────────────┐
│ trade-market               │  │ trade-watchlist      │  │ trade-strategy       │
│                            │  │                      │  │                      │
│ - 股票身份解析             │  │ - 默认股票           │  │ - 策略规则契约       │
│ - K 线 REST API            │  │ - 自选股             │  │ - 回测模型与契约     │
│ - 股票 WebSocket           │  │ - WatchlistService   │  │ - 智能决策模型       │
│ - AkShare HTTP 客户端      │  │ - REST API           │  │                      │
│ - 内存行情缓存             │  │ - PostgreSQL 仓储    │  │ 当前实际回测仍在     │
│ - 交易建议计算             │  │ - MyBatis Mapper     │  │ frontend 中执行      │
│ - 定时行情刷新             │  │ - 启动初始化         │  │                      │
└─────────────▲──────────────┘  └───────────┬──────────┘  └───────────┬──────────┘
              │                             │                         │
              └─────────────────────────────┴─────────────────────────┘
                       watchlist 和 strategy 依赖 market

模块依赖方向：

trade-app -> trade-market
trade-app -> trade-watchlist -> trade-market
trade-app -> trade-strategy  -> trade-market

规则：

1. 所有模块最终打包进 trade-app，一个进程运行。
2. Maven 模块依赖必须保持单向、无循环。
3. Controller、Service、Repository、Client、DTO 和资源文件放在所属业务模块内。
4. trade-market 不得反向依赖 trade-watchlist 或 trade-strategy。
5. 新的机会发现系统适合新增为 trade-research 业务模块。
