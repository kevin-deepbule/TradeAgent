<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tradeagent.repository.mapper.SettingsMapper">
    <update id="createSettingsTable">
        CREATE TABLE IF NOT EXISTS app_settings (
            key TEXT PRIMARY KEY,
            value TEXT NOT NULL DEFAULT '',
            updated_at TIMESTAMPTZ NOT NULL DEFAULT now()
        )
    </update>

    <update id="ensureTimestampDefaults">
        ALTER TABLE app_settings
            ALTER COLUMN updated_at TYPE TIMESTAMPTZ USING updated_at::timestamptz,
            ALTER COLUMN updated_at SET DEFAULT now()
    </update>

    <insert id="insertSettingIfAbsent">
        INSERT INTO app_settings (key, value)
        VALUES (#{key}, #{value})
        ON CONFLICT(key) DO NOTHING
    </insert>

    <select id="selectSettings" resultType="map">
        SELECT key, value
        FROM app_settings
        WHERE key IN
        <foreach collection="keys" item="item" open="(" separator="," close=")">
            #{item}
        </foreach>
    </select>

    <insert id="upsertSetting">
        INSERT INTO app_settings (key, value)
        VALUES (#{key}, #{value})
        ON CONFLICT(key) DO UPDATE SET
            value = excluded.value,
            updated_at = now()
    </insert>
</mapper>
